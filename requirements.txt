# 코어
langgraph
python-dotenv
pyyaml

# Ingest Agent
notion-client          # Notion 연결
requests               # OAuth 토큰 교환
google-api-python-client  # Google Drive 연결
google-auth
pdfplumber             # PDF 파싱
python-docx            # DOCX 파싱
openpyxl               # XLSX 파싱
hwp5                   # HWP 파싱

# Serve Agent
mcp[cli]               # MCP 서버 (FastMCP 포함)
fastapi
uvicorn
openai

# Index Agent
pydantic>=2            # Ingest → Index 입력 스키마 검증
sentence-transformers  # BAAI/bge-m3 임베딩 / BGE reranker
qdrant-client          # 벡터 DB 클라이언트
networkx               # 문서-청크-개념 그래프
pyvis                  # 그래프 HTML 시각화

# Index Agent (팀원 A)
llama-index
llama-index-llms-openai
tiktoken

# Eval Agent (팀원 B)
ragas
