# 코어
langgraph
python-dotenv
pyyaml

# Ingest Agent
notion-client          # Notion 연결
requests               # OAuth 토큰 교환
google-api-python-client  # Google Drive 연결
google-auth
pdfplumber             # PDF 파싱
python-docx            # DOCX 파싱
openpyxl               # XLSX 파싱
hwp5                   # HWP 파싱

# Serve Agent
mcp[cli]               # MCP 서버 (FastMCP 포함)
fastapi
uvicorn
openai

# Index Agent
sentence-transformers  # 임베딩 (테스트용, 무료 로컬)
qdrant-client          # 벡터 DB 클라이언트

# Index Agent (팀원 A)
llama-index
llama-index-llms-openai
tiktoken

# Eval Agent (팀원 B)
ragas
google-genai            # EVAL_LLM_PROVIDER=gemini 일 때만 필요 (OpenAI 토큰 승인 전 임시 대체)
